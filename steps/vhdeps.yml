parameters:
  sources: ['.']
  args: ''
  simulator: 'ghdl'

steps:

- script: |
    vhdeps --stacktrace -i ${{ join(' -i', parameters.sources) }} ${{ parameters.simulator }} -- ${{ parameters.args }}
  displayName: vhdeps (${{ parameters.simulator }})
