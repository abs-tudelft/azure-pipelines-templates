parameters:
  source: '.'
  args: ''

steps:

- script: |
    cmake ${{ parameters.args }} ${{ parameters.source }}
  displayName: CMake

- script: |
    make -j
  displayName: Build

- script: |
    make test
  displayName: Test
